spring:
  datasource:
    url: jdbc:mysql://localhost:3306/internship_system?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 1412goicaul@KID
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
# --- JWT config (replace jwt.secret with secure string) ---
jwt:
  secret: ${JWT_SECRET}
  expirationMs: 1800000
